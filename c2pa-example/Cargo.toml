[package]
name = "cimpl-c2pa"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
cimpl = { path = ".." }
c2pa = "0.75"
serde_json = "1.0"
toml = "0.8"

[build-dependencies]
cbindgen = "0.27"
